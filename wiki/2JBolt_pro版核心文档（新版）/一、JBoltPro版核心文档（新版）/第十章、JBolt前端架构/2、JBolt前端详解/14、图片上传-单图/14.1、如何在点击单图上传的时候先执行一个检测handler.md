### 使用data-check-handler="checkCanUploadImgHandler"


```
function checkCanUploadImgHandler(uploader){
    //在这里做你想做的校测校验 可以直接写同步的代码 也可以执行Ajax.get(url,xxxx)
//执行ajax的时候需要设置为同步执行模式 这样执行完之后 根据返回值判断返回true还是false
//返回true说明可以继续执行图片选择和上传任务 返回false 就是没通过check 自己弹出提示信息即可

    return true;
}
```
